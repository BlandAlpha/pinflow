
## 第五轮：白板交互重做 + 空间（工作/生活分离）

- 白板重写（src/components/views/Whiteboard.tsx）：卡片直接拖拽（window pointer 监听在 pointerdown 时挂载，修掉原先依赖 draft 状态导致监听器永不挂载的 bug）；落点即数据，去掉展示层 avoidOverlap；viewport transform(translate+scale) 取代 overflow-auto。
- 缩放：MIN_ZOOM=1（画布不小于可视区）~3；wheel 以光标为锚（mx-(mx-x)*ratio），指数趋近补间（1-0.04^(dt/190)）；左键拖空白平移（applyNow 立即生效），视口夹紧 [vw-w*z, 0]。
- 空间：DB v5 新增 spaces 表（默认 工作 work / 生活 life 固定 id）+ todos.space_id；AppPrefs.activeSpaceId 持久化到 prefs.json；computeVisible 增加 spaceId 硬过滤；allTags(spaceId) 按空间取标签。
- UI：SpaceSwitcher 放侧栏底栏（在 nav 之外，不破坏冒烟的 nav button 索引）；设置弹窗新增空间管理（改名/图标/颜色/删除随迁任务）；Alt+1..9 快速切换；详情面板可移动任务跨空间。
- 跨窗口同步补全：TODOS_TOGGLE / STEPS_TOGGLE 此前不 broadcast，已加。
- 冒烟新增 --smoke-interact（electron/main/index.ts BOARD_INTERACTION_SCRIPT）：合成 PointerEvent/WheelEvent 校验拖拽/缩放/平移/空间切换/设置弹窗，结果写 .smoke/interact.json。
- 踩坑：electron 必须后台启动否则静默退出（single-instance lock 会吞掉后续实例且无任何输出）；本沙箱 electron-vite build 前台运行输出被吞且可能被 safe-delete 阈值拦截——先 rm -rf app-build 再后台跑 build 可稳定通过；Git Bash 的 taskkill 杀不掉 electron，需用 PowerShell Stop-Process。
- 已提交 1582a54；工作区仍有上一轮未提交的零散改动（README/.gitignore/package.json/windows.ts 等），未混入本次提交。

## 第四轮微调：坐标数学化 + 交互优化

- 坐标系改为数学约定（右=紧急、上=重要），右上=紧急·重要（Q1）。
  DB v4 迁移：旧数据 x 轴 1-x 翻转。priority.ts urgency 同步改 boardX 直接值。
- 白板新增 Ctrl+滚轮缩放（0.5~2.6x），header 显示百分比+重置按钮；网格随 zoom 缩放。
- 主题切换器：删除手绘 SunMoonIcon，用 lucide SunMoon；三按钮→单图标+DropdownMenu 下拉选择。
- 侧栏展开浮层：bg-surface-2/80 + backdrop-blur-md 增强层级感与可读性。
- Today 页面：任务行内联堆叠前 3 步骤（checkbox 可直接切换），超 3 步显示「还有 N 项…」。
- 踩坑：冒烟测试冷启动需 >120s 才能完成全部截图，前台 timeout 需留足余量。
